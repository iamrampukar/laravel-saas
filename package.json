{
  "name": "stripe-sample",
  "version": "0.1.0",
  "dependencies": {
    "@stripe/stripe-js": "^9.0.0",
    "dotenv": "^16.4.5",
    "stripe": "^20.1.0",
    "vite": "^5.2.11"
  },
  "devDependencies": {
    "concurrently": "8.2.2"
  },
  "scripts": {
    "dev": "concurrently \"vite --port 3000 --open\" \"php -S [::1]:4242 server.php\"",
    "build": "vite build"
  }
}